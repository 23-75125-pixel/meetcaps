# Server Configuration

# Port to run the server on (default: 3001)
PORT=3001

# CORS allowed origins (comma-separated)
# Leave empty for development (allows all origins)
# For production, set specific domains like: https://example.com,https://app.example.com
ALLOWED_ORIGINS=*

# Node environment
NODE_ENV=development
